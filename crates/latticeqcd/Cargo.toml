[package]
name = "latticeqcd"
version = "0.1.0"
edition.workspace = true
license = "MIT"
repository.workspace = true

[dependencies]
dirac-operators = { path = "../dirac-operators" }
gaugefields = { path = "../gaugefields" }
measurements = { path = "../measurements", features = ["fermions"] }
num-complex.workspace = true
serde.workspace = true
tenferro-cpu.workspace = true
thiserror.workspace = true
toml.workspace = true
wilsonloop = { path = "../wilsonloop" }
