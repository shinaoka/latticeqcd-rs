[package]
name = "measurements"
version = "0.1.0"
edition.workspace = true
license = "MIT"
repository.workspace = true

[features]
default = []
fermions = ["dep:dirac-operators", "dep:rand"]

[dependencies]
gaugefields = { path = "../gaugefields" }
dirac-operators = { path = "../dirac-operators", optional = true }
num-complex.workspace = true
rand = { workspace = true, optional = true }
thiserror.workspace = true
wilsonloop = { path = "../wilsonloop" }

[dev-dependencies]
npyz.workspace = true
serde_json.workspace = true
tenferro-cpu.workspace = true
tenferro-tensor.workspace = true
